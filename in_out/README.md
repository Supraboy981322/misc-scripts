# copies anything from stdin to stdou

This script can be run without directly running a binary. Just make the source executable and run it like a script.

## why I needed this
I needed this because I was getting some strange behavior with a program's ansi sequences, so I wanted to try forcing it to not print escape sequences (did help solve the problem, for the record).
